# Extension — copy to .env in repo root (never commit .env)

# Google OAuth client ID (Chrome extension type)
# Must match WXT_GOOGLE_OAUTH_CLIENT_ID in the repo root .env
# Format: 123456789-xxxx.apps.googleusercontent.com
WXT_GOOGLE_OAUTH_CLIENT_ID=
